# Flutter project lives in src/ — full .gitignore is at src/.gitignore

# OS junk
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*~

# Editor files at the repo root (per-project settings live in src/.vscode)
.idea/
.vscode/
*.iml
