The third migration script adds an audit_log table so that data doesn't get completely deleted from the DB. If the user updates their DOB, for example, instead of running an UPDATE and removing the old DOB, we call an RPC that updates the DOB but also adds a row to the audit log table that includes the old DOB and a reason for changing it. Required by Prevail for data auditing and integrity checks.